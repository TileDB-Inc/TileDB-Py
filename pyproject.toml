[build-system]
requires = [
    "cython>=0.27",
    "numpy==1.16.* ; python_version < '3.9' and not 'arm' in platform_machine",
    "numpy==1.20.* ; python_version >= '3.9' and 'arm' in platform_machine",
    "numpy ; python_version >= '3.9' and not 'arm' in platform_machine",
    "setuptools>=18.0",
    "setuptools_scm>=1.5.4",
    "wheel>=0.30",
    "pybind11>=2.6.2",
]
